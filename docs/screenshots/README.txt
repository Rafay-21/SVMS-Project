Place screenshot files in this directory.

Required screenshots referenced in docs/user_manual.md and README.md:

  login-page.png              — Login form with username/password fields and 2FA note
  dashboard.png               — Main dashboard with KPI cards and recent activity feed
  register-visitor.png        — Visitor registration form with custom fields and photo upload
  visitor-detail.png          — Visitor profile page with QR code, badge, and visit history
  check-in.png                — Check-in modal with host, department, purpose fields
  appointments-calendar.png   — FullCalendar month view with color-coded appointment blocks
  reports.png                 — Reports page with PDF preview and filter controls
  settings-email.png          — Email settings tab with SMTP configuration form
  blacklist.png               — Blacklist management table with severity badges
  emergency-mode.png          — Emergency control panel with red banner and snapshot button
  kiosk-mode.png              — Kiosk self-service landing screen (tablet layout)
  backup-restore.png          — Backup history table with create/restore/download buttons
  dark-mode.png               — Any page in dark theme to demonstrate dark mode

Recommended format: PNG, 1280x800px or higher, anonymized (no real PII).
